# 贡献指南

感谢你帮助建设 Research Modules。本项目优先接收范围小、接口清楚、能够独立验证的模块。

## 提交新模块

1. 搜索现有模块和 Issue，确认没有功能重复。
2. 从 `MODULE_TEMPLATE.md` 复制模块 README，并补全所有必填项。
3. 将模块放入 `modules/<category>/<module-name>/`。
4. 添加最小可运行示例和覆盖核心行为的测试。
5. 检查许可证、引用、数据隐私和密钥泄露风险。
6. 提交 Pull Request，并说明验证方法与结果。

## 命名规范

- 目录名使用小写 kebab-case，例如 `bootstrap-confidence-interval`。
- 名称应描述功能，避免 `utils`、`helpers`、`misc` 等含糊词。
- 公共函数和参数使用所在语言的通行风格。

## 必备文档

模块 README 必须说明：

- 一句话用途与适用场景；
- 成熟度和维护状态；
- 输入、输出和失败条件；
- 安装方式与最小示例；
- 可复现实验所需的版本、随机性和环境信息；
- 已知限制及不适用场景；
- 数据、算法、论文、模型和代码的引用；
- 模块及第三方依赖的许可证。

## 质量要求

- 默认参数应安全且合理。
- 不允许硬编码本机路径、凭据或私人地址。
- 错误信息应说明用户如何修复问题。
- 测试不得依赖不稳定的外部网络；需要网络时应使用 mock 或单独标记。
- 示例数据应足够小，并且允许公开分发。
- 对随机过程设置种子，或说明无法完全确定复现的原因。

## Pull Request 检查清单

- [ ] 模块解决单一、明确的问题
- [ ] 文档中的示例可以运行
- [ ] 核心逻辑有测试
- [ ] 未提交密钥、隐私数据或大型生成文件
- [ ] 第三方内容已注明来源和许可证
- [ ] 破坏性变更已在说明中标出
- [ ] 新增行为已记录在模块 README 中

## 版本与兼容性

稳定模块遵循语义化版本：修复为 patch，向后兼容的功能为 minor，破坏性更改为 major。实验模块可以快速迭代，但必须在 README 中明确接口可能变化。

## 行为准则

讨论应围绕问题、证据和实现展开。尊重不同背景与经验，提供可执行的反馈，不进行人身攻击、骚扰或歧视。
